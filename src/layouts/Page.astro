---
import Base from './Base.astro';
const { frontmatter } = Astro.props;
---
<Base title={`${frontmatter.title} - The Little Graduates`}>
  <article class="section"><div class="container prose">
    <h1>{frontmatter.title}</h1>
    <slot />
  </div></article>
</Base>
